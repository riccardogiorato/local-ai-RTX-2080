# Task: fix the shopping cart bugs

The project in this directory has a failing test suite. Run the tests,
find the bugs in `lib/cart.js`, and fix them.

Rules:
- You may only edit `lib/cart.js`. Do not modify the tests.
- The test suite must pass completely.

Verify with: `bash verify.sh` (or `node --test test/`).